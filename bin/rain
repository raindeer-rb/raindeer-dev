#!/usr/bin/env ruby

# Call "rain" to use the CLI on your $PATH installed by Bundler.
# Call "bin/rain" to load gems locally from your Gemfile in dev.

require 'bundler/setup'
require 'raindeer/cli'

Rain::CLI.run(ARGV)
